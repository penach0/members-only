<h1>New Post</h1>

<%= form_with model: @post do |f| %>
    <%= render "shared/form_errors", model: @post %>

    <%= f.label :title %>
    <%= f.text_field :title %>

    <%= f.label :body %>
    <%= f.text_area :body %>

    <%= f.submit %>
<% end %>